{
  "name": "@ai-job-hunter/worker",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "playwright:install": "playwright install chromium"
  },
  "dependencies": {
    "@ai-job-hunter/shared": "workspace:*",
    "dotenv": "^16.5.0",
    "playwright": "^1.52.0"
  },
  "devDependencies": { "tsx": "^4.19.4" }
}
